import { font } from "ui/settings";
import { setType } from "ui/mixins";

const h1 = `
  ${setType("h")};
  display: block;
  font-family: ${font.digrotesk};
  font-weight: 600;
`;

export default h1;
