import styled from "styled-components";

import { setSpace } from "ui/mixins";

const Body = styled.div`
  ${setSpace("mth")};
  margin-left: auto;
  margin-right: auto;
  max-width: 1200px;
`;

export default Body;
