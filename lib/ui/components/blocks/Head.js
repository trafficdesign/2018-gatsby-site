import styled from "styled-components";

import { setSpace } from "ui/mixins";

const Head = styled.div`
  ${setSpace("mbh")};
  margin-left: auto;
  margin-right: auto;
  max-width: 1024px;
  text-align: center;
`;

export default Head;
